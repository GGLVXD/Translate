upload:
  files:
    - path: "{lang}.json"
      format: "single-language-json"
      options:
        - REPLACE_TRANSLATION_IF_FOUND 

download:
  files:
    - path: "{lang}.json"
      format: "single-language-json"
